/**
 * @typedef {Object} NodeData
 * @property {boolean} expanded - If the node is expanded
 * @property {boolean} expandable - If the node is expandable
 */

/**
 * @typedef {Object} ExpandCollapseNode
 * @property {string} id - The ID of the node
 * @property {Object} position - The position of the node
 * @property {number} position.x - The x position of the node
 * @property {number} position.y - The y position of the node
 * @property {NodeData} data - The data of the node
 */
